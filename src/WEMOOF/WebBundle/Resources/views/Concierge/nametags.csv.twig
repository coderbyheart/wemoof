WEMOOF-ID	Name	Titel	Tags	Twitter	URL	WEMOOF-Profil
{% for registration in nametags %}
{% if registration.user.public %}~{{ registration.user.id }}{% endif %}	{% if registration.role == 3 %}WEMO♥F VIP{% elseif registration.role == 2 %}WEMO♥F Team{% endif %}	{{ registration.user }}	"{{ registration.user.title }}"	{{ registration.user.tags }}	{{ registration.user.twitter }}	{{ registration.user.url|compacturl }}	{{ shorturls.get(registration.user.id) }}
{% endfor %}